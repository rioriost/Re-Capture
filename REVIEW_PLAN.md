# Re-Capture 設計・コードレビューと修正プラン

- レビュー日: 2026-09-16
- レビューモデル: GPT-6 Astra
- 対象: `1d55285`、バージョン 1.1.0 / ビルド 5
- 範囲: Swift 全9ファイル、Xcode/XcodeGen 設定、権限、ローカライズ、README、プライバシー文書
- 以下の指摘・行番号はレビュー時点の記録。1.1.1での対応状況は末尾に追記する。

## 結論

全面的な作り直しは不要。UI、設定、監視、処理、変換、テンプレートの責務分割と、
設定スナップショットを直列キューへ渡す構成は、この規模に適している。
出力先の明示選択、一時ファイルからの確定、衝突回避、変換不能時の原形式保存も維持したい。

ただし、安全性を支える状態管理が不足している。特に、書き込み中ファイルの削除、
停止後の処理継続、保存後の元ファイル削除失敗を先に修正すべき。
以下に確定指摘8件と、配布形態に依存する要確認事項1件を示す。

優先度は **P1: 次回リリース前に修正**、**P2: 続く修正で対応**。
行番号はレビュー対象コミットを基準とする。

## 確定指摘

### R1 / P1: 一括処理が書き込み途中の元ファイルを削除する

**箇所:** `Recapture/ScreenshotProcessor.swift:28-31, 163-179, 199-208`

一括処理では `isStableFile` を実行しない。出力検証も「通常ファイルでサイズが
0より大きい」だけなので、画像として未完成でも保存成功と判定し、Move では元を削除する。

隔離したファイルで再現した。158バイトの正常なPNGを生成し、先頭8バイトを書いた
時点でファイルを開いたまま一括Moveを実行したところ、処理件数は1、出力は8バイト、
元のパスは削除済みになった。その後、元のファイルハンドルへ残りを書いても、
保存先には反映されない。これは表示上の問題ではなく、実際のデータ欠落である。

**修正方針:** 自動・一括で共通の準備完了判定を使う。処理中は元の同一性・更新を
再確認し、形式に応じた画像/PDFの完全性確認を行う。不完全・更新継続中のものは
元を残して再試行または保留とし、サイズが正という理由だけで削除しない。

### R2 / P1: デバウンスのキャンセルが処理を起動し、停止後も待機済み処理が走る

**箇所:** `Recapture/AppController.swift:22-35, 44-48, 52-74`

`try? await Task.sleep` がキャンセル例外を握り潰し、そのまま `processNow` を呼ぶ。
イベントや設定変更が続くほど古い待機タスクが即座に処理を投入する。
さらに有効状態の確認はキュー投入前だけで、投入済み処理の取り消しや世代確認がない。
古い処理の完了通知は「一時停止中」の表示も上書きする。

実際の `AppController` に監視・処理のテストダブルを接続し、100ms間隔で再設定後に
停止したところ、800msの待機完了前に処理が始まり、停止要求の後にも別の処理が開始した。

**修正方針:** キャンセル時は待機タスクを終了する。実行中1件と追加要求の有無を
管理し、設定変更・停止に世代番号またはキャンセルトークンを導入する。
停止時に新しいファイルの処理を始めず、進行中の保存は安全な境界で終える。
古い世代の結果で現在の状態表示を変更しない。

### R3 / P1: 保存成功・元削除失敗を単なる失敗にして、重複出力と誤った状態表示を生む

**箇所:** `Recapture/ScreenshotProcessor.swift:43-54, 148-155, 179-196`

出力確定後に元ファイルの削除が失敗すると、生成マーカーも処理記録も残さず
`.failed` になる。出力自体は残るため、次の走査で同じ元から別名の出力を作る。
また `.failed` の理由と件数は捨てられ、「0件処理しました」とだけ表示される。

読み取り可能・削除不可の入力ディレクトリで2回処理すると、元を残したまま出力が
2件でき、両方とも生成マーカーがなく、両回の表示は `Processed 0 screenshots` だった。
出力先が存在しない場合も同じ表示になり、未処理と失敗を区別できない。

**修正方針:** 「出力未確定」「出力確定・元保持」「完了」を区別する。
出力確定を記録してから元削除へ進み、削除失敗時は出力を作り直さない。
`ProcessResult` に失敗・保留・元保持と個別の理由を持たせ、UIで明示する。
記録失敗や生成マーカー書き込み失敗も扱いを定義する。

### R4 / P2: Copy の処理済み記録が再起動で消え、同じ元を再コピーする

**箇所:** `Recapture/ScreenshotProcessor.swift:4-7, 229-237`

元ファイルの処理済み記録はメモリ上の `Set` だけで、永続マーカーは出力側にしかない。
Copy では元が残るため、新しい `ScreenshotProcessor` は同じ元を未処理と判断する。
起動時の全件走査により、アプリを再起動するたびに重複が増える。

同じ元に対する処理件数は「初回1、同じインスタンスの再実行0、
新しいインスタンスで1」となり、出力が2件になった。

**修正方針:** 元の識別情報・内容の版・確定済み出力を永続記録し、再起動後も
冪等性を維持する。記録対象の情報は元削除前に取得する。
明示的な一括再処理と、自動処理の重複防止は別の規則にする。

### R5 / P2: 「新規」の自動処理と「既存」の一括処理が分離されていない

**箇所:** `Recapture/AppController.swift:22-31`、
`Recapture/ScreenshotProcessor.swift:14-31`、
`Recapture/SettingsStore.swift:42-44`

起動・再設定時にディレクトリ全体を走査し、監視開始以前のファイルを除外しない。
README は新しいスクリーンショットの自動処理と既存ファイルの一括処理を区別しているが、
実装では初回のフォルダ設定だけで古いスクリーンショットも処理対象になる。
既定値が Move / WebP なので、意図せず古い原本を移動・変換する影響がある。

古い作成日時・更新日時を設定したファイルも `bulk: false` で処理され、Move では
元のパスが削除された。初回の既存ファイル取り込みを意図するなら、仕様と確認UIが必要。

**修正方針:** 初回の既存ファイルは自動処理せず、一括処理への明示的な導線を設ける
方針を推奨する。起動前・一時停止中に増えたファイルの扱いも明文化する。
単なる作成日時比較だけに頼らず、初期一覧・処理記録・監視の再開状態を使う。

### R6 / P2: 「macOSに適用」する前の編集値が監視・処理に使われる

**箇所:** `Recapture/SettingsView.swift:73-103, 134-139`、
`Recapture/RecaptureApp.swift:58-60`、
`Recapture/SettingsStore.swift:100-114`

UIの編集中データと、実際に適用済みの `screenshotDefaults` が同じ値である。
保存フォルダを選ぶだけで監視先が切り替わり、名前や形式を編集するだけでも
`onChange` が再設定を起動する。一方でmacOSへの書き込みは「適用」ボタンまで行わない。
この間、macOSの実際の出力先・形式と、アプリが監視する設定が一致しない。

**修正方針:** 編集用の値と適用済みの値を分ける。適用の成否が確定してから
監視を一度だけ切り替える。外部で変更されたmacOS設定の再取得経路も設ける。

### R7 / P2: テンプレートの空白入力で隠しファイルを作り、隣接トークンにも誤出力がある

**箇所:** `Recapture/TemplateRenderer.swift:5-19, 22-30`

空文字のフォールバック判定がサニタイズ前にあるため、空白だけの入力は最終的に
空のベース名になる。Move / Original で `"   "` を使用すると、元を削除して
出力先に `.png` という隠しファイルを作った。

また、各トークンをDateFormatterの引用符で囲む方式では、隣接した引用符が
文字としてのアポストロフィになり、`{app}{sequence}` は `Finder'0001` になる。

**修正方針:** 日付書式の引用規則を考慮したトークン解析に置き換える。
サニタイズ後の空名・隠し名・ファイル名上限を処理開始前に検証し、無効な入力を
UIに明示する。既存プリセットの出力は維持する。

### R8 / P2: ブックマーク保存失敗でもフォルダ選択成功を返す

**箇所:** `Recapture/SettingsStore.swift:29-34, 85-109, 155-186`

`didSet` 内のブックマーク保存が失敗しても、呼び出し側は成功メッセージを上書きし、
`true` を返す。現在のURLだけが更新され、永続化された権限と一致しなくなる。
取り外し可能なボリュームの切断など、選択から保存までの間の失敗も適切に通知できない。

存在しないディレクトリではブックマーク作成が `NSCocoaErrorDomain:260` で失敗するが、
実際の `setDestinationURL` は `true` と `Output folder selected` を返した。
また、復元時は解決できたブックマークでも stale なら無条件に破棄している。

**修正方針:** ブックマーク生成・保存が成功してから設定を確定し、失敗を呼び出し側へ
返す。stale でも解決可能なURLは権限を確認してブックマークを更新し、回復できない場合は
対象フォルダの再選択を案内する。スコープ開始の成否とアクセス終了の対応も管理する。

## D1 / P1相当の要確認事項: App Sandbox とmacOS設定変更の整合性

**箇所:** `Recapture/Models.swift:135-168`、
`Recapture/SettingsStore.swift:112-114`、
`Recapture/Recapture.entitlements:5-10`

現行アプリは App Sandbox 有効だが、`com.apple.screencapture` という別ドメインに
直接 `CFPreferencesSetAppValue` で書き込む設計である。定義済みの権限は
ユーザー選択ファイルとアプリスコープのブックマークで、共有設定ドメインへの権限はない。

Apple は共有設定ドメインへのアクセス用に
`com.apple.security.temporary-exception.shared-preference.read-write` を文書化している。
フォルダ選択権限だけで、配布版がシステム側の設定を変更できるとは判断できない。
さらに同期の戻り値を捨て、常に「macOS設定を更新しました」と表示する点は確定している。

これは**署名・Sandbox有効の配布構成で実挙動を確認すべき設計リスク**であり、
このレビューでは配布版の変更失敗そのものを再現したとは扱わない。
ユーザーの実際のスクリーンショット設定は変更していない。

**対応方針:** macOS 26 / 27 の署名済み構成で、別プロセスが見るシステム側の値と、
実際のスクリーンショット出力の両方を確認する。同じプロセスでのread-backだけを
成功根拠にしない。直接変更を維持する場合は必要権限とApp Store審査条件を確認する。
維持できない場合は、その機能を無効化してmacOS標準UIでの設定手順へ誘導する。
未確認の例外権限追加やSandbox無効化を安易な解決策にはしない。

参考:
[Apple: Shared Preference Domain Temporary Exceptions](https://developer.apple.com/library/archive/documentation/Miscellaneous/Reference/EntitlementKeyReference/Chapters/AppSandboxTemporaryExceptionEntitlements.html)

## 修正の実施順序と完了条件

| 順序 | 対象 | 実施内容 | 完了条件 |
| --- | --- | --- | --- |
| 1 | R1・R3 | 転送結果を型で分け、準備完了確認・出力確定・処理記録・元削除の順序を整理する。最小のテストターゲットを `project.yml` に追加する。 | 書き込み中の元を削除しない。元削除失敗で出力が増えない。出力先不在・書き込み不可・容量不足・変換失敗を区別して通知する。 |
| 2 | R2 | キャンセル対応デバウンス、要求の集約、処理世代、ファイル単位の停止境界を導入する。 | 連続イベントでは静穏期間後に1回だけ走る。停止後に待機ジョブや次のファイルを開始しない。旧世代の結果が設定・状態表示を上書きしない。 |
| 3 | R4・R5 | 処理記録を永続化し、初回・再開・一括処理の対象規則を分離する。 | Copy後に再起動しても重複しない。初回の既存ファイルは明示同意なしに移動しない。異常終了後に確定済み出力を再作成しない。 |
| 4 | R6・R8・D1 | 編集値と適用値を分離し、設定・権限の更新を失敗可能な操作にする。配布版で成立するmacOS設定連携を確定する。 | 未適用編集で監視先が変わらない。ブックマーク保存失敗で成功表示しない。stale・ボリューム切断・設定適用失敗から再選択または再試行へ進める。 |
| 5 | R7 | テンプレート解析と最終ファイル名の検証を修正する。 | 既存プリセット、隣接トークン、引用符、空白のみ、禁止文字、長いタイトルを扱える。意図しない隠しファイルや空のベース名を作らない。 |

D1の配布可否調査は順序1から並行して進め、機能維持/変更の判断を順序4までに確定する。
各修正は回帰ケースと同じ変更単位にまとめ、先に大規模な抽象化だけを行わない。

追加の完了条件:

- 出力先と入力先が同じ場合、入力先の子フォルダが出力先の場合も、生成物を再取り込みしない。
- 拡張属性を保存できないファイルシステムでも、再起動・再走査による重複を防ぐ。
- Move / Copy、原形式 / 変換 / 原形式フォールバックで、元保持と結果通知が一貫する。
- 新規の永続処理記録について保存項目・保持方針を決め、プライバシー文書へ反映する。
- 対象範囲、停止の意味、macOS設定連携の制約をREADMEへ反映し、新しい通知を日英で揃える。

## 確認範囲と限界

ファイル処理の再現はmacOS 27.0上の専用ディレクトリで行い、既存のユーザーファイルは
移動・変換していない。`ScreenshotProcessor` / `Models` / `TemplateRenderer` は
実際のソースを使用した。転送の再現は Original に限定し、変換器は呼ばれないことを
保証するテストダブルに置き換えたため、画像エンコーダ自体の動作確認とは区別する。
制御処理の再現も実際の `AppController` を使用し、監視・処理先のみを置き換えた。
R6はUIバインディングと監視切り替え経路の静的確認による。

macOS 26実機、App Store署名版、実際の画面撮影、外部ボリューム切断、容量不足は
このレビューでは実行していない。D1を含む配布・OS依存の完了条件は、修正時の
統合確認として残す。

## 1.1.1での対応

| 指摘 | 実装した対応 |
| --- | --- |
| R1 | 自動・一括の準備完了判定を共通化。入力の一時コピーを検証し、PNG/JPEG終端、PDF終端、ImageIOの全フレーム状態を確認。処理中の元の変更を検出した場合は保持する。 |
| R2 | キャンセル対応の待機、要求集約、処理世代、ファイル境界の停止、古い結果の破棄を導入。再試行は上限付きで保留対象だけに限定する。 |
| R3 | 出力公開前に処理履歴を保存し、公開後の状態も確定してから元を削除。元削除失敗は元保持として通知し、次回は削除だけを再試行する。 |
| R4 | ファイル識別情報・処理結果・生成物の記録をApplication Supportへ永続化。中断した出力の公開を復旧し、再起動でも重複を防ぐ。 |
| R5 | 新しい監視フォルダの初回一覧は自動処理から除外し、明示的な一括処理で扱う。その後に増えた/変更されたファイルは、停止・終了中のものも再開時に処理する。 |
| R6 | 編集値と適用値を分離し、適用済み構成だけを監視切り替えに使う。監視先の選択はmacOSの保存先変更とは独立した操作として明示する。 |
| R7 | 引用符を解釈するトークン解析と最終名の検証を導入。空名・隠し名・長すぎる名前を拒否し、隣接トークンと既存プリセットを維持する。 |
| R8 | ブックマーク保存後にのみURLを確定。stale更新とアクセス開始失敗の巻き戻しを実装し、同じフォルダの再選択も構成更新として通知する。 |
| D1 | Sandbox有効または判定不能なら直接のmacOS設定書き込みを無効化。標準「スクリーンショット」UIへ誘導し、選択済み監視先を独立して保持する。例外権限の追加やSandbox無効化は行わない。 |

転送・制御・設定・テンプレートのXCTestターゲットを追加した。
リリース前の追加レビューで見つかった「一括Copyの再試行が成功済みファイルも複製する」
問題と「同じURLの権限再選択では監視が再開しない」問題にも、対応する回帰ケースを追加した。
拡張属性なしの同一フォルダ・子フォルダ・シンボリックリンク経由の出力も対象に含める。

確認結果: macOS 27.0 / Xcode 27.0で **74テスト成功、失敗・スキップ0件**。
Release構成はarm64 / x86_64のUniversalビルドが成功し、製品のバージョン1.1.1 /
ビルド6を確認した。日英95キーの対応と書式引数、XcodeGen出力の再生成一致も確認した。

この変更はmacOS 27上のローカル確認を対象とする。macOS 26実機、App Storeへの
アップロード・審査・配信、実ユーザー設定を書き換える操作は実行しない。
GitHub Releaseはソースリリースであり、署名・公証済みアプリやApp Store配信とは区別する。
